# Copy to .env and set your VM2 (node-b) IP.
# Used for ORDERS_URL and for Prometheus scrape targets (after render_prometheus.sh).
VM2_IP=192.168.56.102

# Shared secret for payments /admin/* and autotriage_agent remediation (set the same on VM2).
ADMIN_TOKEN=change-me-class-demo
