# Copy to .env and set your VM1 (node-a) IP.
# Used for OTEL, Jaeger, and Prometheus URLs.
VM1_IP=192.168.56.101

# Must match VM1 .env (payments admin + autotriage_agent).
ADMIN_TOKEN=change-me-class-demo
